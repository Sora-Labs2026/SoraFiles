import {readdir,readFile,stat,writeFile,mkdir} from 'node:fs/promises';import {resolve,join,relative} from 'node:path';import {createHash} from 'node:crypto';
const target=process.argv[2];if(!['x86_64-pc-windows-msvc','aarch64-apple-darwin','x86_64-apple-darwin','x86_64-unknown-linux-gnu'].includes(target))throw Error('Unknown native target');
const root=resolve('desktop/native/target',target,'release/bundle'),artifacts=[];
async function visit(dir){for(const entry of await readdir(dir,{withFileTypes:true})){if(entry.isSymbolicLink())continue;const path=join(dir,entry.name);if(entry.isDirectory())await visit(path);else if(/\.(dmg|deb|AppImage|exe)$/.test(entry.name)){const info=await stat(path),hash=createHash('sha256').update(await readFile(path)).digest('hex');artifacts.push({file:relative(root,path).replaceAll('\\','/'),bytes:info.size,sha256:hash});}}}
await visit(root);if(!artifacts.length)throw Error('No installer artifacts were produced');await mkdir('.artifacts',{recursive:true});
await writeFile('.artifacts/native-candidate-'+target+'.json',JSON.stringify({schema:1,target,version:'0.1.0',commit:process.env.GITHUB_SHA||null,run:process.env.GITHUB_RUN_ID||null,builtAt:new Date().toISOString(),publicRelease:false,artifacts,remaining:'Native runtime, full processing, licensing, install/uninstall and release clearance'},null,2));console.log(`Recorded ${artifacts.length} candidate artifact hashes for ${target}`);
