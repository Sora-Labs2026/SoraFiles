{
  "schema": 1,
  "macos": {
    "signing": "signed",
    "notarized": true,
    "installation": "standard",
    "updaterEligible": false,
    "architectures": ["arm64", "x64"],
    "ownerSteering": "September 17 recovery prompt: require Developer ID signing, notarization and native validation before publication. Ad-hoc candidates are local/CI tests only."
  },
  "linux": {
    "initialBuildTarget": "ubuntu-22.04",
    "architectures": ["x64"],
    "packages": ["deb", "AppImage"],
    "otherDistributions": "Publish compatibility only after actual installation and runtime tests."
  },
  "publication": "No artifact becomes downloadable merely because it builds. Require native runtime, engine, licensing and installer checks."
}
