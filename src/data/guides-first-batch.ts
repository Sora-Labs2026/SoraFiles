import type { Guide, GuideBlock } from './guides';
const h = (id: string, text: string): GuideBlock => ({ type: 'heading', level: 2, id, text });
const p = (text: string): GuideBlock => ({ type: 'paragraph', text });
const note = (text: string): GuideBlock => ({ type: 'callout', text });
const link = (text: string, href: string) => ({ text, href });
const para = (...parts: (string | { text: string; href: string })[]): GuideBlock => ({ type: 'linked-paragraph', parts });
const list = (...items: string[]): GuideBlock => ({ type: 'list', items });
const table = (caption: string, headers: string[], rows: string[][]): GuideBlock => ({ type: 'table', caption, headers, rows });
const ref = (title: string, url: string) => ({ title, url });
type BatchInput = Omit<Guide, 'author' | 'publisher' | 'status' | 'indexable' | 'locale' | 'canonical' | 'funnelStage'>;
const batch = (guide: BatchInput): Guide => ({ ...guide, author: { name: 'Sora Labs', type: 'Organization', url: 'https://sorafiles.com/about' }, publisher: { name: 'Sora Labs', url: 'https://sorafiles.com/' }, status: 'published', indexable: true, publishedAt: '2026-09-10', locale: 'en', canonical: `https://sorafiles.com/guides/${guide.slug}`, funnelStage: 'awareness' });

export const firstGuides: Guide[] = [
  batch({
    slug: 'what-local-file-processing-means', title: 'What Does “Processed Locally in Your Browser” Mean?',
    description: 'Understand local versus server file processing, browser memory, downloads, offline limits, and what a local-processing claim does—and does not—guarantee.',
    primaryQuery: 'what does local file processing mean', secondaryQueries: ['browser based file processing', 'do browser file tools upload files', 'local vs server file processing', 'how browser file processing works'], searchIntent: 'informational', category: 'Privacy & Security', tags: ['privacy', 'browser', 'local processing'], relatedTools: ['compress-pdf', 'image-converter'], relatedGuides: ['remove-metadata-before-sharing-files'],
    introduction: 'Local processing means the browser on your device reads and transforms a selected file instead of first sending it to a remote processing server. The website can still download the code needed for that work and make ordinary website requests. Local file processing describes where the operation happens; it does not mean the whole website is disconnected from the internet.',
    body: [
      h('local-or-server', 'Local processing vs server processing'),
      table('Where the work happens', ['Question', 'Local processing', 'Server processing'], [
        ['Who reads and transforms the file?', 'Software running on your device.', 'Software on a remote server after an upload.'],
        ['Whose resources do the work?', 'Your device’s processor and memory.', 'Primarily the server’s processor and memory.'],
        ['What needs a connection?', 'The page and any uncached code, models, or other requests.', 'Uploading the input, processing requests, and receiving the result.'],
        ['What should you check?', 'Actual upload behavior, other requests, and local storage.', 'Upload handling, retention, access, and deletion policies.'],
      ]),
      p('For example, rotating a PDF locally lets your browser create the rotated copy from the selected original. A server-based service receives the PDF, creates a copy remotely, and sends the result back. Either approach can have additional features; the label alone does not describe every data flow.'),
      h('selecting-a-file', 'What happens when you select a file?'),
      p('A browser file picker gives the page access to the files you choose. Selection and upload are separate actions: JavaScript can read the selected bytes locally, but a website can also send those bytes over the network. A button labeled “Choose file” does not establish which behavior follows.'),
      p('Processing may hold input bytes, decoded images, page previews, and output bytes in memory at the same time. A temporary object URL, usually starting with blob:, lets the page display or download browser-held data. Creating that URL is not an upload or a public sharing link. Releasing it lets the browser reclaim associated resources; it is not a promise of secure erasure from device memory or storage.'),
      h('engines-and-limits', 'Why does a local tool still download things or use memory?'),
      p('JavaScript runs application logic. WebAssembly lets browsers run compiled processing engines, while browser features can decode images and draw previews. None of these technologies automatically prevents a website from making network requests.'),
      p('A tool may need to download a reusable OCR language file or a background-removal model before it can work. Receiving that shared resource is different from sending your document to a server. The processing itself still consumes CPU and RAM: a compressed photo can expand into a much larger pixel buffer, and a long PDF may need many page previews. Large jobs can be slow or exceed a phone’s available memory.'),
      h('offline', 'What does “offline after first load” depend on?'),
      p('Offline use depends on the exact page, processing engine, and required resources being available in the browser cache. Opening a homepage once does not necessarily cache every tool. Browsers may clear cached data, private browsing may retain less, and a different language or model may need a fresh download. Test the specific workflow offline before depending on it.'),
      h('privacy-limits', 'What local processing does not guarantee'),
      list('It does not prove that a site has no analytics, cookies, contact services, or other network traffic.', 'It does not remove metadata or visible personal information from the output.', 'It does not protect a downloaded file from other people or software with access to your device.', 'It does not eliminate risks from browser extensions, compromised code, or an insecure device.'),
      para('Check a service’s current privacy disclosures and the behavior of the particular tool. If you plan to share the result, also review ', link('file metadata and visible content', '/guides/remove-metadata-before-sharing-files'), '.'),
      h('sorafiles', 'How SoraFiles uses local processing'),
      para('SoraFiles processes supported files in the browser and does not send their contents or results to a SoraFiles processing server. Its ', link('Privacy page', '/privacy'), ' separately describes website requests and anonymous tool-usage events. The contact form is an intentional exception to general website use: information you submit there is sent to the contact service.'),
      para('The ', link('Open Source page', '/open-source'), ' identifies the browser libraries and application source. These are first-party implementation disclosures, not an independent security certification. Try a representative workflow such as ', link('Compress PDF', '/pdf'), ' or ', link('Image Converter', '/image-converter'), ', or browse ', link('All Tools', '/tools'), '.'),
    ],
    references: [ref('MDN: Using files from web applications', 'https://developer.mozilla.org/en-US/docs/Web/API/File_API/Using_files_from_web_applications'), ref('MDN: Creating object URLs', 'https://developer.mozilla.org/en-US/docs/Web/API/URL/createObjectURL_static'), ref('MDN: WebAssembly concepts', 'https://developer.mozilla.org/en-US/docs/WebAssembly/Guides/Concepts'), ref('MDN: Using service workers', 'https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API/Using_Service_Workers'), ref('SoraFiles privacy disclosures', 'https://sorafiles.com/privacy')],
  }),
  batch({
    slug: 'pdf-compression-quality-vs-file-size', title: 'PDF Compression: File Size vs Quality Explained',
    description: 'Learn why PDF compression sometimes blurs images, why some files barely shrink, and how to check a smaller copy for sharing, screen reading, or printing.',
    primaryQuery: 'pdf compression quality vs file size', secondaryQueries: ['why does compressing a pdf reduce quality', 'pdf compression dpi', 'how much should i compress a pdf', 'pdf compression settings'], searchIntent: 'informational / troubleshooting', category: 'PDF', tags: ['compression', 'quality', 'resolution'], targetTool: 'compress-pdf', relatedTools: [], relatedGuides: ['scanned-pdf-vs-searchable-pdf'],
    introduction: 'PDF compression can reduce file size without changing the visible page, or it can trade image detail for fewer bytes. The result depends on what occupies the file and which compression methods are used. There is no single quality setting that suits every PDF: keep the original and judge the smaller copy against its intended use.',
    body: [
      h('large-pdfs', 'Why are some PDFs so large?'),
      p('A PDF can contain text, vector drawings, raster images, embedded fonts, and other document data. A short brochure with large photographs can outweigh a long text-only report. Scanned pages often store the entire page—including the letters—as pixels, whereas native text and vector lines can stay sharp without a full-page image.'),
      para('This distinction explains why a blurrier compressed scan may also have blurrier text. See ', link('scanned PDF vs searchable PDF', '/guides/scanned-pdf-vs-searchable-pdf'), ' for the difference between page images and a text layer.'),
      h('what-changes', 'What changes during compression?'),
      p('Structural compression rewrites or packs PDF data more efficiently. Image compression may reduce pixel dimensions or encode images with stronger lossy settings. Other products may remove document features or rebuild pages as images. These methods have different consequences; “compress PDF” does not identify one universal process.'),
      p('Resolution describes how many image pixels cover a physical area, often discussed as DPI in PDF settings. For example, an image 1,200 pixels wide placed across six inches provides 200 pixels per inch. Reducing its pixel dimensions removes detail. Enlarging it afterward cannot restore the missing information.'),
      p('JPEG-style lossy encoding also discards information. Stronger settings can produce block patterns, halos around letters, or smeared texture even when pixel dimensions stay the same. Small labels and scanned fine print often expose these changes before a large photograph does.'),
      h('choose-a-priority', 'Choose quality for the actual destination'),
      table('Priorities for a compressed copy', ['Goal', 'Recommended priority', 'What to inspect'], [
        ['Email or a size-limited form', 'Meet the limit while keeping the content usable.', 'Small text, stamps, and images after downloading.'],
        ['Screen reading', 'Balance file size and clarity.', 'Normal reading size and zoomed diagrams.'],
        ['Printing', 'Preserve fine detail.', 'Fine text, photographs, lines, and a sample printed page.'],
        ['Archive or master copy', 'Preserve the original.', 'Keep the original rather than replacing it with a compressed derivative.'],
      ]),
      p('If a form requires a smaller attachment, start with moderate compression and increase only as needed. A document that meets the byte limit but makes an identification number unreadable has failed the practical task. Keep an unmodified master separately so you can make a different copy later.'),
      h('limited-savings', 'Why did the file barely shrink?'),
      p('Already compressed images and efficiently stored text leave less redundant data to remove. Embedded resources or protected document features can also limit safe changes. Recompressing an existing small PDF does not guarantee further savings; a new encoding may even be larger. A dramatic reduction on one scanned brochure does not predict the result for another file.'),
      h('sorafiles-compression', 'What the current SoraFiles compressor does'),
      para(link('Compress PDF with SoraFiles', '/pdf'), ' uses a strength control. It first attempts a structural reduction. Additional image compression is limited to eligible documents without detected native text or protected content; the current checks compare candidate pages and keep an accepted smaller result. Signed documents are returned unchanged.'),
      p('The tool may return the original when a candidate is not smaller or does not pass its checks. Those checks reduce the risk of a bad result, but they are not a guarantee of identical appearance or preservation of every PDF feature. SoraFiles’ current behavior should not be assumed to describe another compressor.'),
      h('inspect-result', 'Check the downloaded file before sending it'),
      list('Open the downloaded PDF and confirm its page count and order.', 'Zoom into the smallest text, diagrams, faces, and any scanned signatures.', 'Try selecting or searching text if that mattered in the original.', 'Check links, forms, and other features you need. For printing, inspect a printed sample.', 'Compare the actual file size with the destination’s limit, then keep both the original and the accepted copy.'),
    ],
    references: [ref('Adobe: PDF optimization settings', 'https://helpx.adobe.com/acrobat/desktop/create-documents/optimize-pdfs/pdf-optimizer-settings.html'), ref('JPEG: JPEG 1 standard overview', 'https://jpeg.org/jpeg/'), ref('SoraFiles: Compress PDF', 'https://sorafiles.com/pdf')],
  }),
  batch({
    slug: 'why-pdf-to-word-formatting-changes', title: 'Why Does PDF to Word Formatting Change?',
    description: 'Understand why PDF conversion changes fonts, columns, and tables, and how editable reconstruction differs from preserving each page as an image in Word.',
    primaryQuery: 'why does pdf to word formatting change', secondaryQueries: ['pdf to word formatting problems', 'why converted pdf looks different in word', 'pdf to docx layout changes', 'editable pdf vs visual pdf conversion'], searchIntent: 'troubleshooting / informational', category: 'Troubleshooting', tags: ['DOCX', 'layout', 'conversion'], targetTool: 'pdf-to-word', relatedTools: ['pdf-ocr', 'word-to-pdf'], relatedGuides: ['scanned-pdf-vs-searchable-pdf'],
    introduction: 'PDF-to-Word formatting changes because a PDF describes a fixed visual page, while a Word document describes editable content that can flow and be laid out again. A converter often has to infer paragraphs, columns, and tables from positioned text. The PDF may not contain enough information to recreate the original document’s structure.',
    body: [
      h('different-formats', 'Why a page that looks right can convert badly'),
      p('In a PDF, separate pieces of text can be drawn at specific coordinates. A human sees one paragraph or two columns, but the stored operations do not necessarily identify that relationship. WordprocessingML, the document format inside DOCX, represents structures such as paragraphs, text runs, and tables. Converting between these models involves interpretation.'),
      p('For example, a two-column newsletter may store text fragments in an order that differs from normal reading order. A converter can accidentally join a left-column sentence to a right-column heading. Likewise, numbers aligned to look like a table may have no actual table structure to recover.'),
      h('fonts', 'Why do fonts and line breaks change?'),
      p('A PDF may embed a font or only a subset of its characters. That does not guarantee the same usable font is available to the converted Word document. Font substitution, character widths, paragraph spacing, and page margins can change line wrapping. A small difference near the beginning can move content onto another page.'),
      h('visual-vs-editable', 'Visual preservation vs editable conversion'),
      table('Two different conversion goals', ['Approach', 'What you get', 'Main tradeoff'], [
        ['Visual preservation', 'A rendered image of each PDF page placed in a DOCX.', 'The page can look similar, but its text is not ordinary editable Word text.'],
        ['Editable reconstruction', 'Recognized or extracted text rebuilt into Word content.', 'You can edit the text, but layout and document structure may change.'],
      ]),
      p('A page-image document can be useful when a recipient specifically needs a DOCX containing the visual pages. It is usually a poor choice for rewriting paragraphs. Editable reconstruction suits text revisions, provided you allow time to review headings, lists, tables, and reading order. Neither approach recovers the original source file perfectly.'),
      h('scans-and-ocr', 'What changes when the PDF is scanned?'),
      para('An image-only scan has no usable text for a normal text extractor. OCR estimates the characters from pixels. It can misread a zero as the letter O or merge adjacent columns, and recognizing the words does not recover the original styles and document structure. Learn how ', link('a searchable scan differs from a native PDF', '/guides/scanned-pdf-vs-searchable-pdf'), '.'),
      h('sorafiles-options', 'Choose the appropriate SoraFiles output'),
      para('The current ', link('SoraFiles PDF to Word tool', '/pdf-to-word'), ' offers editable and visual outputs. Editable conversion extracts available text, uses OCR when needed, and constructs Word paragraphs with inferred headings, lists, and simple tables. It does not promise reconstruction of the original layout or all graphical content. Visual output puts rendered PDF pages into DOCX as images.'),
      p('Choose editable output if changing the wording matters most. Choose visual output if preserving the page’s visible arrangement matters more than editing its text. Check the selected output option before conversion and review a complex page before relying on the whole document.'),
      h('repair-checklist', 'How to reduce surprises'),
      list('Ask for the original DOCX if it is available; it is a better editing source than a converted PDF.', 'Check the first page, a dense page, and every complex table before editing the whole file.', 'Compare column reading order, numbered lists, headers, footnotes, and page breaks.', 'Review names, dates, totals, and identifiers after OCR. A plausible-looking word can still be wrong.', 'After editing, create a PDF and compare the final pages again before sharing.'),
      para('If you only need to find or extract scanned text, ', link('PDF OCR', '/pdf-ocr'), ' may be enough. After editing a DOCX, use ', link('Word to PDF', '/word-to-pdf'), ' to prepare a fixed-page copy, then inspect it in a PDF viewer.'),
    ],
    references: [ref('Microsoft: Opening PDFs in Word', 'https://support.microsoft.com/en-us/word/opening-pdfs-in-word'), ref('Microsoft: Structure of a WordprocessingML document', 'https://learn.microsoft.com/en-us/office/open-xml/word/structure-of-a-wordprocessingml-document'), ref('Adobe: Font handling in PDF conversion', 'https://helpx.adobe.com/acrobat/desktop/create-documents/explore-advanced-conversion-settings/font-handling-distiller.html'), ref('SoraFiles: PDF to Word', 'https://sorafiles.com/pdf-to-word')],
  }),
  batch({
    slug: 'jpg-vs-png-vs-webp', title: 'JPG vs PNG vs WebP: Which Image Format Should You Use?',
    description: 'Compare JPG, PNG, and WebP for photographs, screenshots, transparent logos, and web delivery, including lossless options and practical compatibility limits.',
    primaryQuery: 'jpg vs png vs webp', secondaryQueries: ['png vs jpg', 'webp vs jpg', 'best image format', 'best image format for transparency', 'jpg png webp differences'], searchIntent: 'informational comparison', category: 'File Formats', tags: ['JPG', 'PNG', 'WebP'], targetTool: 'image-converter', relatedTools: ['compress-image', 'heic-to-jpg'], relatedGuides: ['remove-metadata-before-sharing-files'],
    introduction: 'Choose JPG for ordinary photographs and broad compatibility, PNG when lossless pixel preservation or transparency matters, and WebP for efficient web delivery when the destination accepts it. The best choice depends on the image and its use. No format is always smallest or always best.',
    body: [
      h('format-comparison', 'Compare the capabilities before choosing'),
      table('JPG, PNG, and WebP at a glance', ['Feature', 'JPG', 'PNG', 'WebP'], [
        ['Compression in ordinary use', 'Lossy photographic encoding.', 'Lossless.', 'Lossy or lossless.'],
        ['Alpha transparency', 'No.', 'Yes.', 'Yes.'],
        ['Animation', 'Not in an ordinary JPEG image.', 'APNG supports animation; ordinary PNG exports may be still images.', 'Supported by the format; not every exporter retains it.'],
        ['Photographs', 'A practical sharing choice.', 'Can be much larger.', 'Useful for web delivery; compare the actual output.'],
        ['Screenshots and UI text', 'Lossy edges can show artifacts.', 'Good for sharp text and exact pixels.', 'Lossless mode can suit sharp graphics.'],
        ['Logos', 'Poor fit if transparency is required.', 'Good for transparent raster logos.', 'Good when transparency and destination support fit.'],
        ['Editing intermediates', 'Avoid repeated lossy saves.', 'Useful for lossless raster intermediates.', 'Lossless is possible; verify editor support.'],
        ['Compatibility', 'Widely accepted by devices and upload forms.', 'Widely supported, though some forms require JPG.', 'Supported by modern browsers; older apps and specific forms may reject it.'],
      ]),
      h('photos', 'Why JPG works well for photographs'),
      p('Ordinary JPG encoding discards some information to store a photograph compactly. Smooth changes in color and natural texture can tolerate that tradeoff better than tiny lettering. Strong compression can create visible blocks and edge halos. Repeatedly editing and saving a lossy copy can accumulate damage, so keep an original or lossless working file.'),
      p('A transparent logo cannot remain transparent in JPG. The conversion must provide a background color, which may differ from the page where you later place it. Check the result against its intended background before sending it.'),
      h('sharp-graphics', 'When is PNG the better choice?'),
      p('PNG preserves the encoded pixel values without lossy image compression and supports alpha transparency. That makes it useful for screenshots, diagrams, line art, and raster logos. A screenshot of small interface text is a concrete case where a slightly larger PNG can be more useful than a JPG with fuzzy letters.'),
      p('Lossless encoding does not undo earlier damage. Converting a blurred JPG to PNG preserves the already-blurred pixels. PNG also does not preserve an editor’s layers, vectors, or every piece of source metadata just because its image compression is lossless.'),
      h('web-delivery', 'When should you use WebP?'),
      p('WebP offers both lossy and lossless encoding, plus transparency and animation support. It is useful for website images when your publishing system and visitors’ software support it. Actual size and appearance depend on the source, dimensions, and encoding settings; a WebP extension alone does not tell you whether the file is lossless.'),
      p('For an email attachment or an application form, the recipient’s accepted formats matter more than a theoretical size advantage. If a form requires JPG, use a real JPG export. Renaming photo.webp to photo.jpg changes the filename, not the encoded image.'),
      h('practical-workflow', 'Choose a format, then check the exported image'),
      para('The ', link('SoraFiles Image Converter', '/image-converter'), ' produces JPG, PNG, or WebP. Its current workflow exports the selected image or displayed frame/page, so do not assume that animation or every page in a multipage input survives conversion. Keep layered and animated originals when those features matter.'),
      para('Start with the destination’s accepted formats, then check dimensions, edge clarity, transparency, and file size. Use ', link('Compress Image', '/compress-image'), ' when you want to reduce an image’s size, or ', link('HEIC to JPG', '/heic-to-jpg'), ' when an iPhone photo needs a more widely accepted format.'),
      para('Changing format can also change metadata, but conversion is not a reliable promise of privacy cleanup. Before sharing, review ', link('metadata and visible information', '/guides/remove-metadata-before-sharing-files'), ' separately.'),
    ],
    references: [ref('MDN: Image file types and formats', 'https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Formats/Image_types'), ref('W3C: PNG specification, third edition', 'https://www.w3.org/TR/png-3/'), ref('Google: WebP compression techniques', 'https://developers.google.com/speed/webp/docs/compression'), ref('JPEG: JPEG 1 standard overview', 'https://jpeg.org/jpeg/')],
  }),
  batch({
    slug: 'remove-metadata-before-sharing-files', title: 'File Metadata: What It Is and What to Check Before Sharing',
    description: 'See what image and PDF metadata can contain, what SoraFiles removes, and why metadata cleanup does not redact visible information or guarantee anonymity.',
    primaryQuery: 'file metadata before sharing', secondaryQueries: ['what metadata is stored in files', 'photo metadata privacy', 'pdf metadata privacy', 'remove metadata before sharing', 'does a pdf contain personal information'], searchIntent: 'informational / privacy', category: 'Privacy & Security', tags: ['metadata', 'EXIF', 'sharing'], targetTool: 'metadata-remover', relatedTools: [], relatedGuides: ['what-local-file-processing-means'],
    introduction: 'File metadata is information stored alongside a file’s main content, such as an author name, capture time, camera model, or document title. The fields vary by format and by the software that created the file. Not every file contains sensitive metadata, but checking it can prevent sharing details you did not intend to include.',
    body: [
      h('common-fields', 'What metadata might a file contain?'),
      table('Examples to inspect', ['File type', 'Possible fields', 'What to remember'], [
        ['Photographs', 'EXIF capture time, camera/device details, orientation, software, and GPS coordinates when recorded.', 'Location may be absent; orientation can affect display.'],
        ['PDFs', 'Title, author, subject, keywords, creator, producer, and creation/modification dates.', 'A PDF can also contain other metadata and document features.'],
        ['Office documents', 'Author, last editor, document properties, company, and custom properties.', 'Comments and tracked changes are separate things to review.'],
      ]),
      p('For example, a photograph can show an ordinary wall while its location field identifies where it was taken. Conversely, a screenshot may contain little capture metadata but visibly display a name in a notification. The appropriate check depends on the file, not an assumption that all metadata is sensitive.'),
      h('not-redaction', 'Metadata removal is not redaction'),
      note('Removing metadata does not remove a visible name, address, face, signature, account number, or other information in the document or image. It also does not guarantee anonymity.'),
      p('If information is visibly present, use a suitable redaction workflow and verify the saved result. Covering text with a shape can leave the original text recoverable. Metadata cleanup also does not necessarily remove comments, attachments, hidden document content, or revision history.'),
      h('supported-cleanup', 'What the current SoraFiles cleanup removes'),
      para('The current ', link('Metadata Remover', '/metadata-remover'), ' handles specific fields and containers in supported formats. Its scope is narrower than removing every possible identifying trace:'),
      table('Current supported cleanup scope', ['Format', 'Fields or containers handled', 'Important limit'], [
        ['PDF', 'Standard title, author, subject, keywords, creator, and producer fields; document-level XMP metadata.', 'Creation and modification dates are reset to an epoch date, not deleted. Custom fields and other document content may remain.'],
        ['JPG', 'APP1 and APP13 metadata segments and comment segments.', 'Other segments remain; removing EXIF orientation can change how a viewer displays the photo.'],
        ['PNG', 'Text metadata, EXIF, and modification-time chunks.', 'Image data and other chunk types are not a universal metadata wipe.'],
        ['WebP', 'EXIF and XMP chunks.', 'Image content and other data remain.'],
        ['Supported Office files', 'Selected core properties, company/manager properties, and custom properties.', 'This does not remove comments, tracked changes, or embedded content.'],
      ]),
      p('These statements describe the current implementation, not every metadata cleaner. PDF cleanup can leave a date such as January 1, 1970; that represents the reset value, not the original document’s creation date. Inspect the downloaded copy rather than interpreting a “removed” count as proof that no metadata exists.'),
      h('reexports', 'Does a screenshot or re-export remove metadata?'),
      p('It can discard some fields, preserve others, or add new fields from the exporting application. A screenshot also changes the image and may include surrounding interface content. Re-exporting a PDF can affect quality, text selection, or document features. Neither operation is a universal substitute for inspection.'),
      h('sharing-check', 'A practical check before sharing'),
      list('Keep an original copy, then inspect the file properties available in your viewer or editor.', 'Review the visible pages or pixels, including backgrounds, notifications, and small text.', 'Check comments, tracked changes, attachments, and hidden content where the format supports them.', 'Apply supported cleanup to a separate copy and reopen it. Check metadata, appearance, and orientation again.', 'Check the filename and the destination too; information can be exposed outside the file itself.'),
      para('You can ', link('remove supported metadata with SoraFiles', '/metadata-remover'), ' on your device. Read the ', link('Privacy page', '/privacy'), ' for its data-handling disclosures, and ', link('what local processing means', '/guides/what-local-file-processing-means'), ' for the distinction between keeping a file on your device and making its eventual shared copy safe to disclose.'),
    ],
    references: [ref('CIPA: Exif 3.0 overview', 'https://www.cipa.jp/std/documents/e/Exif3.0-Overview_E.pdf'), ref('Adobe: PDF properties and metadata', 'https://helpx.adobe.com/acrobat/desktop/edit-documents/edit-pdf-properties/pdf-properties.html'), ref('W3C: PNG specification, third edition', 'https://www.w3.org/TR/png-3/'), ref('SoraFiles: Metadata Remover', 'https://sorafiles.com/metadata-remover')],
  }),
  batch({
    slug: 'scanned-pdf-vs-searchable-pdf', title: 'Scanned PDF vs Searchable PDF: What’s the Difference?',
    description: 'Learn how image-only scans, native PDFs, and OCR text layers differ, why recognition makes mistakes, and how to check search and copied text before relying on it.',
    primaryQuery: 'scanned pdf vs searchable pdf', secondaryQueries: ['what is a searchable pdf', 'how does pdf ocr work', "why can't i search text in pdf", 'does scanned pdf contain text'], searchIntent: 'informational / troubleshooting', category: 'PDF', tags: ['OCR', 'scans', 'searchable PDF'], targetTool: 'pdf-ocr', relatedTools: ['pdf-to-word', 'doc-scanner'], relatedGuides: ['why-pdf-to-word-formatting-changes'],
    introduction: 'A scanned PDF often contains pictures of pages rather than selectable text. A searchable PDF contains a text layer that software can search, select, copy, or index. OCR can add recognized text to a scanned PDF while preserving the visible page image, but the recognized words may contain mistakes.',
    body: [
      h('three-types', 'Image-only, native, and searchable scanned PDFs'),
      table('How the three kinds of PDF differ', ['Feature', 'Image-only scan', 'Native digital PDF', 'OCR/searchable scan'], [
        ['Selectable text', 'Usually none.', 'Usually available when text is encoded accessibly.', 'Available where recognized text was added.'],
        ['Search', 'Cannot search the pictured words as text.', 'Usually searches stored text.', 'Searches the recognized text, including its errors.'],
        ['Appearance', 'A page image.', 'Text, vectors, images, or a mixture.', 'Often the same page image with a hidden text layer.'],
        ['OCR required', 'Yes, to recognize the pictured words.', 'Usually not for readable native text.', 'Already performed, though a poor result may need another attempt.'],
        ['Recognition errors', 'No recognition layer yet.', 'No OCR errors in native text; extraction can still have problems.', 'Possible even when the visible scan looks correct.'],
      ]),
      p('A PDF can mix these types: a digital report may include a scanned appendix. Try searching for a distinctive visible word and copying a sentence from several pages. A failed search alone is not definitive proof of an image-only scan; text encoding, permissions, or a poor OCR layer can also interfere.'),
      h('ocr-layer', 'How does an invisible text layer work?'),
      p('OCR—optical character recognition—estimates characters from an image. A PDF exporter can store the recognized words invisibly while leaving the scan visible. Search then uses those stored words, not a fresh reading of the pixels. Good alignment places the hidden words over their matching image locations so selection and highlighting feel natural.'),
      p('For example, a scan visibly showing invoice number 1080 might have 1O8O in its OCR layer. Searching for the correct number can fail even though the page image is unchanged. Copying the number into a spreadsheet carries the recognition error with it.'),
      h('recognition-errors', 'Why does OCR make mistakes?'),
      list('Low resolution loses character detail; enlarging a blurry scan cannot recover the missing strokes.', 'Skew, motion blur, shadows, noise, and compression artifacts make letter shapes harder to separate.', 'An inappropriate recognition language can produce the wrong words or characters.', 'Handwriting and unusual fonts are less predictable than clear printed text.', 'Columns, tables, footnotes, and rotated labels can confuse reading order even when individual letters are recognized.'),
      p('A searchable PDF is not automatically a well-structured or accessible document. Recognizing words does not necessarily create proper headings, table relationships, reading order, or document tags. OCR also does not recreate the original Word source.'),
      h('sorafiles-ocr', 'What to expect from SoraFiles PDF OCR'),
      para('The current ', link('PDF OCR tool', '/pdf-ocr'), ' runs recognition locally. It keeps pages with detected readable native text and recognizes scanned pages. Choosing the appropriate language and supplying a clear, straight scan improves the conditions for recognition; accuracy still needs checking.'),
      note('Current searchable-PDF export has limitations: its added hidden text uses a restricted Latin character encoding and is not positioned word by word over the scan. Non-Latin recognized text may be missing from PDF search, and selection highlights may not align with the visible words. Use text output when you need recognized characters that the PDF export cannot retain, and verify the result.'),
      p('These export limits are separate from whether the OCR engine recognized the text. A useful text result can still produce an incomplete searchable PDF. Pages with existing native text are not subject to that added-text encoding in the same way, but they still need a practical search and copy check.'),
      h('check-results', 'Check the result before relying on it'),
      list('Search several distinctive words and numbers from different pages.', 'Copy a paragraph into a plain-text editor to inspect characters and reading order.', 'Compare names, dates, totals, and identifiers with the original image.', 'Check pages with columns, tables, handwriting, and any language changes separately.', 'Keep the original scan alongside the OCR result so uncertain text can be checked later.'),
      para('For a clearer starting image, use ', link('Doc Scanner', '/doc-scanner'), ' to adjust the document capture. If you need editable paragraphs, compare ', link('PDF to Word', '/pdf-to-word'), ' with the ', link('visual and editable conversion tradeoffs', '/guides/why-pdf-to-word-formatting-changes'), ' before choosing an output.'),
    ],
    references: [ref('Adobe: Recognize text in scanned documents', 'https://helpx.adobe.com/acrobat/desktop/create-documents/scan-documents-to-pdfs/recognize-text.html'), ref('Tesseract: Improving recognition quality', 'https://tesseract-ocr.github.io/tessdoc/ImproveQuality.html'), ref('Tesseract: Frequently asked questions', 'https://tesseract-ocr.github.io/tessdoc/FAQ.html'), ref('SoraFiles: PDF OCR', 'https://sorafiles.com/pdf-ocr')],
  }),
];
